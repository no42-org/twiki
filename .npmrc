# Fail the install on an unsupported Node rather than warning. The floor is
# real: node:sqlite is absent below 22.5.0 and only reaches release-candidate
# stability on v24, and the store depends on it.
engine-strict=true
